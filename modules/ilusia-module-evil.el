;; evil
(use-package evil
  :ensure t
  :config
  (evil-mode 1))
;; (require 'evil)
;; bindings
;; (global-set-key (kbd "C-<escape>") #'evil-mode)

(provide 'ilusia-module-evil)
;;; ilusia-module-evil.el ends here
